<div class="jumbotron">
  {{#if user}}
    <p>Mail has been sent sucessfully</p>
  {{else}}
   <p class="lead">Please sign in as admin</p>
   <a class="btn btn-primary btn-large" href="{{signInUrl}}">Click here to login</a>
  {{/if}}
</div>